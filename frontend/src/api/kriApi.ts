import type { KriDefinition } from '../types/KriDefinition';
import type { KriReading } from '../types/KriReading';

export const getKris = async (): Promise<KriDefinition[]> => {
  return [];
};

export const getKriReadings = async (_kriId: string): Promise<KriReading[]> => {
  return [];
};
